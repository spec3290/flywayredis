spring:
  datasource:
    url: jdbc:mysql://localhost:3307/study
    username: study
    password: study

  data:
    redis:
      host: localhost
      port: 6379

  config:
    import: optional:file:.env[.properties]

server:
  port: 8081

jwt:
  secret: ${JWT_SECRET:local-development-only-jwt-secret-key-change-me}

app:
  auth:
    cookie-secure: false
